%this function switches to the analyse shift mode during the analysis of
%the inpainting

function[] = analyseshiftbutton_callback(varargin)
        
    %get the figure  
    fA = get(varargin{1},'Parent');

    
end